const { resolve } = require('path')

// Retrieve the base directory used to resolve most paths.
// This is the repository root directory, which can be overridden with
// `build.base`
const getBaseDir = async function(cwd, repositoryRoot, { build: { base = repositoryRoot } }) {
  return resolve(cwd, base)
}

module.exports = { getBaseDir }
